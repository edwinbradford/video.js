VideoJS Neue is a theme for VideoJS replacing the style of the player and minor functionality changes.

It contains:

01. CSS rewrite including replacing icon font with svgs.

02. Minor Javascript change to two files:

	src/js/control-bar/live-display.js // Replaced "Live" text with a graphic
	src/js/control-bar/playback-rate-menu-button.js // Commented out button functionality
	
ToDo: Move Javascript changes to a plugin if possible so the JavaScript matches the VideoJS source code.